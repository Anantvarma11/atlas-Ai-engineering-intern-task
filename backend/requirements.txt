fastapi==0.140.13
python-multipart==0.0.18
uvicorn[standard]==0.51.0
pandas==3.0.5
rapidfuzz==3.14.5
numpy==2.5.1
qdrant-client[fastembed]
networkx

# optional LLM adjudication pass (pipeline/llm_adjudicate.py) — Cerebras is
# OpenAI-API-compatible, so the official `openai` client works against it.
# Both are no-ops at runtime if CEREBRAS_API_KEY is unset.
openai==2.50.0
python-dotenv==1.2.2

# test dependencies
pytest==9.1.1
httpx==0.28.1
openpyxl
